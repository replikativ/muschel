.cpcache/
.lsp/
.calva/
.clj-kondo/
target/
out/
*.log
.nrepl-port

# shadow-cljs / npm
.shadow-cljs/
node_modules/

# build outputs — regenerated by `npm run build:*`, served from unpkg
# after `clojure -T:build npm-publish`. Not committed.
# (dist/muschel.d.ts IS committed — hand-written TypeScript types.)
dist/muschel.js
dist/playground/
