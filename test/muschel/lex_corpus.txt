"echo hello"
"echo hello world"
"ls -la"
"pwd"
"true && echo ok"
"false || echo fallback"
"true && false || echo recovered"
"cmd1; cmd2; cmd3"
"echo a | tr a A"
"echo hi | head -c 1 | wc -c"
"yes | head -5"
"FOO=bar"
"FOO=bar BAZ=qux"
"FOO=bar echo $FOO"
"export FOO=bar"
"export FOO BAR BAZ"
"unset FOO"
"local x=1"
"declare -i x=42"
"declare -a arr"
"readonly PI=3.14"
"if [ -f /etc/hostname ]; then echo yes; fi"
"if [ -d /tmp ]; then echo dir; else echo no; fi"
"if true; then echo y; elif false; then echo z; else echo no; fi"
"if [[ \"$x\" == foo ]]; then echo match; fi"
"for i in 1 2 3; do echo $i; done"
"for i in a b c; do echo step $i; done"
"for ((i=0; i<5; i++)); do echo $i; done"
"while read line; do echo got $line; done"
"until [ -f /tmp/done ]; do sleep 1; done"
"case $1 in start) echo go ;; stop) echo halt ;; *) echo unknown ;; esac"
"foo() { echo from foo; }"
"function bar { echo bar $@; }"
"foo 1 2 3"
"echo $((2 + 3))"
"echo $((1 << 8))"
"echo $((x = 5, x * 2))"
"echo $((x > 3 ? 1 : 0))"
"echo $(date +%Y)"
"X=$(echo nested)"
"X=`echo old-style`"
"echo \"x=$X y=$Y\""
"echo 'literal $X'"
"echo $'tab\\there\\n'"
"echo \"$(echo nested ok)\""
"echo ${VAR}"
"echo ${VAR:-default}"
"echo ${VAR:=assigned}"
"echo ${VAR:?error msg}"
"echo ${VAR:+alt}"
"echo ${#VAR}"
"echo ${VAR##*/}"
"echo ${VAR%.txt}"
"echo ${VAR/foo/bar}"
"echo ${VAR//foo/bar}"
"echo ${VAR^^}"
"echo ${VAR,,}"
"echo *"
"echo file.{txt,md,rst}"
"echo {1..10}"
"echo {a..z}"
"echo ~/Documents"
"echo ~root"
"cat <<EOF\nhello\nworld\nEOF"
"cat <<-EOF\n\thello\n\tworld\n\tEOF"
"cat <<'EOF'\nno $expansion\nEOF"
"cat <<<\"hello herestring\""
"cmd > out.txt"
"cmd >> append.log"
"cmd < input.txt"
"cmd 2> err.txt"
"cmd 2>&1"
"cmd &> all.log"
"cmd 1>&2"
"cmd <&0"
"cmd > out.txt 2> err.txt"
"sleep 10 &"
"cmd1 & cmd2 & wait"
"(cd /tmp && pwd)"
"{ echo a; echo b; }"
"! true"
"! [ -f /tmp/x ]"
"[ $# -gt 0 ] && echo args"
"[[ -n \"$VAR\" && \"$VAR\" != skip ]] && echo ok"
"[[ \"abc\" =~ ^[a-z]+$ ]] && echo matches"
"set -e"
"set -euo pipefail"
"set -x"
"shift 2"
"return 1"
"exit 0"
"echo line1\\\nline2"
"echo \"multi\\\n line\""
"git log --oneline | head -5"
"git diff HEAD~1 -- '*.clj'"
"find . -name '*.txt' -exec grep -l foo {} \\;"
"find /tmp -type f -mtime -1 -print0 | xargs -0 ls -la"
"grep -rE 'pattern' src/ | head -20"
"sed -i 's/foo/bar/g' file.txt"
"awk -F: '{print $1}' /etc/passwd | head"
"curl -sL https://example.com/ | head -10"
"npm install"
"npm run build"
"node -e \"console.log('hello')\""
"python3 -c \"print(2+2)\""
"clojure -M:test"
"clojure -Sdeps '{:deps {nrepl/nrepl {:mvn/version \\\"1.3.0\\\"}}}' -M -e '(println :ok)'"
"bb -e \"(+ 1 2 3)\""
"docker run --rm -it ubuntu:latest bash"
"ssh user@host 'uptime'"
"tar -czf out.tgz dir/"
"if command -v jq >/dev/null 2>&1; then echo present; fi"
"[[ \"$PATH\" =~ /usr/local/bin ]] && echo ok"
"trap 'cleanup' EXIT INT TERM"
"echo \"escaped backslash: \\\\\""
"echo 'It'\\''s fine'"
"printf '%s\\n' a b c"
"printf 'pi = %.2f\\n' 3.14159"
